<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.grades24">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="QUIZZER APPLICATION"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".StudyMaterialActivity2"></activity>
        <activity android:name=".FinalResultActivity" />
        <activity android:name=".StudyMaterialsActivity" />
        <activity android:name=".StartQuizActivity" />
        <activity android:name=".RuleActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.login.activity_login_teacher"
            android:label="@string/title_activity_login_teacher" />
        <activity android:name=".SignUp" />
        <activity android:name=".HomeActivity" />
    </application>

</manifest>